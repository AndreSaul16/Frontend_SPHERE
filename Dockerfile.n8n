FROM n8nio/n8n:latest

ENV N8N_PORT=5678
ENV N8N_PROTOCOL=https
ENV DB_TYPE=sqlite

# Persist n8n workflows, credentials, and SQLite database
VOLUME /home/node/.n8n

EXPOSE 5678

HEALTHCHECK --interval=30s --timeout=10s --retries=3 CMD wget -qO- http://localhost:5678/healthz || exit 1
