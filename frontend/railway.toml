[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# No start command here — Dockerfile CMD ["nginx", "-g", "daemon off;"] handles process
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
